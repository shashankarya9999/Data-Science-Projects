Rain Prediction in Australia using **Classification Machine Learning algorithms**: `linear regression`, `k-nearest neighbour(KNN)`, `decision tree`, `support vector machine(SVM)` and `logistic regression`.
